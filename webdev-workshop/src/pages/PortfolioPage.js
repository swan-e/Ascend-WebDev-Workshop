import '../App.css';
import { Box, Button, Card, Typography } from '@mui/material';

export default function PortfolioPage() {
  return (
    <Typography variant="h5">Portfolio Page</Typography>
  );
}